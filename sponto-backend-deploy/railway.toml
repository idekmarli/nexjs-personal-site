[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn server:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:${PORT:-8000}"
healthcheckPath = "/api/dashboard"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
